version: 0.2
phases:
  install:
       commands:
         - echo dummy started on `date`
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - test_file.txt
    - appspec.yml
  discard-paths: yes